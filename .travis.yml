os: linux
dist: xenial

language: python
cache: pip

python:
  - "3.7"

services:
  - docker-compose

# before_install:
# 

script:
- pwd
- ls -lah
- docker-compose --version
- docker-compose up -d
- python3 -V
- docker-compose down 
